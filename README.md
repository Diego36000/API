# Hewoooo

meow :3

## Setup
Instalar pnpm o npm
Windows (pnpm): `iwr https://get.pnpm.io/install.ps1 -useb | iex`

```bash
cd frontend
pnpm i
cd ..
```
Y ejecutar el script de start:
    - Windows: `./start.ps1`
    - Linux: `./start.sh`